% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acti_sleep_guiders.R
\name{acti_sleep_fuse_labels}
\alias{acti_sleep_fuse_labels}
\title{Fuse multiple sleep/wake labels with STAPLE}
\usage{
acti_sleep_fuse_labels(
  labels,
  threshold = 0.5,
  tolerance = 1e-06,
  max_iterations = 200L
)
}
\arguments{
\item{labels}{A data frame, matrix, or tibble of logical/binary sleep-label
columns, such as the output of \code{acti_sleep_labels()}.}

\item{threshold}{Posterior sleep-probability threshold for the fused label.}

\item{tolerance, max_iterations}{STAPLE convergence controls.}
}
\value{
A tibble with \code{sleep_probability} and binary \code{sleep} columns. If
\code{labels} includes \code{time} or \code{epoch}, that index is retained. The fitted
STAPLE parameters are attached as the \code{staple} attribute.
}
\description{
STAPLE estimates a latent consensus sleep label and each input labeler's
sensitivity and specificity using expectation-maximisation.
}
\examples{
labels <- data.frame(
  sleep_l5 = c(FALSE, TRUE, TRUE, FALSE),
  sleep_fixed = c(FALSE, TRUE, FALSE, FALSE)
)
acti_sleep_fuse_labels(labels)
}
