% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acti_sleep_guiders.R
\name{acti_sleep_ensemble}
\alias{acti_sleep_ensemble}
\title{Run a sleep-label ensemble from raw or epoch data}
\usage{
acti_sleep_ensemble(
  data,
  guiders = NULL,
  epoch = "5 seconds",
  sib_method = "vanHees2015",
  fusion = c("staple", "majority"),
  sleepwindow_type = c("SPT", "TimeInBed"),
  ...
)
}
\arguments{
\item{data}{Raw triaxial or regular epoch-level data. Epoch data should have
a \code{sib} column or a conventional activity column.}

\item{guiders}{Guider names. Defaults depend on whether raw acceleration is
available.}

\item{epoch}{Raw-data aggregation epoch.}

\item{sib_method}{SIB method, passed to \code{acti_sleep_sib()}.}

\item{fusion}{Consensus rule: \code{"staple"} or \code{"majority"}.}

\item{sleepwindow_type}{SPT or TimeInBed overlap rule.}

\item{...}{Passed to the individual guider functions.}
}
\value{
A list of time-indexed data. \code{epoch_data} includes the van Hees
\code{sib}; each guider has a \code{label_data} element with \code{time} and \code{window};
\code{labels} has \code{time} plus one logical \verb{sleep_<guider>} column per guider;
\code{fused} has \code{time}, probability, and consensus label; and \code{label_data}
combines all these analysis columns.
}
\description{
This convenience workflow detects or accepts sustained inactivity bouts,
runs compatible sleep guiders, creates one sleep label per guider, then
produces a consensus. Raw triaxial data use \code{HDCZA}, \code{HorAngle}, and a fixed
22:00--08:00 window by default; \code{L5} is included only when the data span its
requested duration. Epoch data use \code{NotWorn} and the fixed window, adding
\code{L5} when possible. Set \code{guiders} to choose a subset explicitly.
}
\examples{
\donttest{
raw <- actiread::acti_read_gt3x(actiread::acti_example_gt3x(), verbose = FALSE)
result <- acti_sleep_ensemble(raw)
}
}
