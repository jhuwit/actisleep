% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acti_sleep_guiders.R
\name{acti_sleep_labels}
\alias{acti_sleep_labels}
\title{Create sleep/wake labels for multiple guiders}
\usage{
acti_sleep_labels(sib, guiders, sleepwindow_type = c("SPT", "TimeInBed"))
}
\arguments{
\item{sib}{Logical or 0/1 sustained-inactivity-bout classification.}

\item{guiders}{A named list of \code{acti_sleep_guider} objects. All guider
windows must use the same epoch grid as \code{sib}.}

\item{sleepwindow_type}{The SPT or TimeInBed overlap rule.}
}
\value{
A tibble with one logical \verb{sleep_<guider>} column per guider.
}
\description{
Create sleep/wake labels for multiple guiders
}
\examples{
data <- actimetrics::acti_count_data
data <- data[rep(seq_len(nrow(data)), length.out = 24 * 60), ]
data$time <- data$time[1] + (seq_len(nrow(data)) - 1) * 60
guiders <- list(L5 = acti_sleep_l5(data), fixed = acti_sleep_setwindow(data))
labels <- acti_sleep_labels(data$counts == 0, guiders)
}
