% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acti_sleep_guiders.R
\name{acti_sleep_label}
\alias{acti_sleep_label}
\title{Convert a sleep guider to epoch-level sleep/wake labels}
\usage{
acti_sleep_label(sib, guider, sleepwindow_type = c("SPT", "TimeInBed"))
}
\arguments{
\item{sib}{Logical or 0/1 sustained-inactivity-bout classification at the
same regular epochs as \code{guider$window}.}

\item{guider}{An \code{acti_sleep_guider} object.}

\item{sleepwindow_type}{Whether the guider approximates sleep-period time
(\code{"SPT"}) or time in bed (\code{"TimeInBed"}).}
}
\value{
A logical vector: \code{TRUE} for sleep and \code{FALSE} for wake.
}
\description{
A guider defines the likely main sleep window; it is not a sleep/wake
classifier itself. This function applies GGIR's final overlap step to a
sustained-inactivity-bout (\code{sib}) classification. With the default
\code{"SPT"} rule, an entire SIB is sleep when any part overlaps the guider. With
\code{"TimeInBed"}, an SIB must be wholly inside the guider.
}
\examples{
data <- actimetrics::acti_count_data
data <- data[rep(seq_len(nrow(data)), length.out = 24 * 60), ]
data$time <- data$time[1] + (seq_len(nrow(data)) - 1) * 60
guider <- acti_sleep_l5(data)
sib <- data$counts == 0
sleep <- acti_sleep_label(sib, guider)
}
